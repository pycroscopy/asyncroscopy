# LEGACY (AutoScript 1.15 bundle). The canonical install is `uv sync` from the
# repo root, which resolves vendor wheels via [tool.uv.sources] in pyproject.toml.
# This pip path is kept only for the self-contained 1.15 wheels in this folder
# (run pip from within this directory so --find-links="." finds them).
# In case you have trouble installing Pillow due to the zlib library use: Pillow -C zlib=disable
# When adding a new dependency, don't forget to regenerate environment bundle. (see manual)

--find-links="."

pillow
numpy
matplotlib
colorama
opencv_python
lxml
defusedxml
h5py
pytest
pyside6
autoscript_core
autoscript_tem_microscope_client
autoscript_tem_microscope_client_tests
autoscript_tem_toolkit
autoscript_tem_snippets_tests
thermoscientific_logging